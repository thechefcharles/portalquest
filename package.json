{
  "name": "portalquest-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --ignore=node_modules --ignore=.git --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
